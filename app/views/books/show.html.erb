<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<h2>Title</h2>
<p><%= @book.title %></p>

<h2>Body</h2>
<p><%= @book.body %></p>

<p><%=link_to "edit",edit_book_path%></p>
<p><%=link_to "back",books_path%></p>

<%= link_to "Destroy", destroy_book_book_path(@book), method: :delete, data: { confirm: "Are you sure?" } %>